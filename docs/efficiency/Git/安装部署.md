# 安装部署

## 编译安装

```bash
wget https://mirrors.edge.kernel.org/pub/software/scm/git/git-2.20.1.tar.gz

./configure --prefix=/home/ampmon/git --with-zlib=/home/ampmon/kong_install/zlib

make && make install
```

